#Sipago - v 0.1

En el archivo /objetos/conexion.php coloca los datos para conectarte a la base de datos(direccion ip o url,usuario y contraseña).
En la carpeta _.db se encuentra la base de datos para restaurar en el servidor mysql.
El programa viene con un .htaccess incluido, modificalo a tu gusto.
Este contiene instrucciones para usar compresion de archivos, incrementando la velocidad de carga de la aplicacion.
Tambien define las paginas 403 y 404 para manejo de estos 'errores'.

La aplicacion fue hecha en un editor libre (Geany) en un sistema operativo libre (Debian),
fue probada en navegadores libres (Firefox, Chromium, Midori, Konqueror).
